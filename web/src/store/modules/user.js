const state = {
  logged: false
};

const getters = {};

const actions = {};

const mutations = {
  setLogged(state, status) {
    state.logged = status;
  }
};

export default {
  state,
  getters,
  actions,
  mutations
};
